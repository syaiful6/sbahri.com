{
  "name": "sbahri-blog",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run build:css && hugo server -D",
    "build": "bun run build:css && hugo --minify && bun run highlight",
    "build:css": "bun run ./scripts/build-css.ts",
    "highlight": "bun run ./scripts/highlight.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.1.15",
    "sass": "^1.83.5",
    "shiki": "^1.29.1"
  }
}
