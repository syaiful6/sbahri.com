{
  "name": "sbahri-blog",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run build:css && bun run ./scripts/dev.ts",
    "build": "bun run build:css && hugo --minify && bun run highlight",
    "build:css": "bun run ./scripts/build-css.ts",
    "highlight": "bun run ./scripts/highlight.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.1.15",
    "sass": "^1.83.5",
    "shiki": "^1.29.1"
  },
  "dependencies": {
    "dom-serializer": "^3.1.1",
    "domutils": "^4.0.2",
    "glob": "^13.0.6",
    "htmlparser2": "^12.0.0",
    "wrangler": "^4.45.2"
  }
}
